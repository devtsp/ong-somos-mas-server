//const { Activities } = require('./models/activities.js'); 
//(Model activities todavía no creado)

const addActivity = ({name, content}) => {
    //Activity.create({name, content});
};

module.exports = { addActivity };